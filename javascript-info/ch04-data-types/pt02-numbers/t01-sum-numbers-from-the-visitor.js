'use strict';

let firstNumber = Number(prompt('Enter the first number'));
let secondNumber = Number(prompt('Enter the second number'));

alert(`The sum of your numbers: ${firstNumber + secondNumber}`);
